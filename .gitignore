node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.log

# Xcode user-specific
xcuserdata/
*.xcuserstate

# Claude Code — per-developer local settings (may contain secrets in
# tool-allow patterns). The shared .claude/settings.json is still tracked.
.claude/settings.local.json
